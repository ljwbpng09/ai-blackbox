# Vibe Coding · Prompt 模板库

> 从 AI Blackbox 项目提炼，覆盖"启动 → 开发 → 上线 → 参赛"全流程。
> 使用方法：把 `[方括号]` 内容替换成你的具体情况，约束部分一般不需要改。

---

## 模板 1 · 项目骨架生成

> 场景：从零启动新项目，让 AI 一次性搭好可跑的脚手架。

```
我在做 [项目类型，如 AI×Web3 / SaaS工具 / 数据看板]。

项目是两个独立 package（不要 monorepo）：
- [worker/agent]（Node + TypeScript + tsx 直跑）
- web（Next.js App Router 最新版）

技术栈：TypeScript + [核心库，如 viem / openai / axios] + Next.js + Tailwind + [图表库]

请帮我生成项目骨架：

1. 目录结构严格按这个来：
[你的目录树]

2. 只实现"最小可跑闭环"：
- [核心功能1，如：worker 每 60 秒拉一次数据，写入 web/public/snapshot.json]
- [核心功能2，如：web 的 /dashboard 读 snapshot.json 画折线图]
- 其他模块先留 TODO 注释 + console.log 占位

3. 所有外部调用（HTTP / RPC / LLM）都包 try/catch：
   失败重试 1 次后跳过本轮并打 warn 日志，绝不让进程崩。

4. 生成 .env.example（列全所有 key，不填真实值）和 .gitignore（排除 .env）

5. LLM client 用 OpenAI 兼容写法，model / baseURL / key 全从 .env 读
```

---

## 模板 2 · LLM 决策函数（Function Calling）

> 场景：让 AI 写一个"接收数据 → 调用工具 → 返回决策"的核心决策模块。

```
基于 llm.ts 的 decide()，帮我在 [目录] 写 [场景名称，如 alertOnAnomaly]，要求：

1. 两个工具：
   - [工具A名](参数): [做什么，如 trigger_alert — 发通知并上链]
   - [工具B名](参数): [做什么，如 record_only — 只记日志不打扰]

2. system prompt 用这段：
[你的系统 prompt，越具体越好，包含：角色 / 目标 / 触发条件 / 不触发条件 / 边界值]

3. 每轮把这些喂给 decide()：
   - [当前数据，如 tokenId、当前价格]
   - [历史数据，如 最近 60 条价格数组]
   - [状态数据，如 上次告警时间，用于去重]

4. 接进 [主循环文件]：每轮拿到新数据后跑一次决策。

5. LLM 调用失败不能让主循环崩，try/catch 兜底。
```

**配套 system prompt 框架：**

```
你是一个负责 [具体场景] 的 AI Agent。
你的目标是：[一句话目标]。
你可以使用的工具：[工具名 + 1 行说明]。

下面是当前的数据和上下文：
- 当前数据：[结构化数据]
- 历史数据（最近 N 条）：[结构化数据]
- 当前状态：[如上次触发时间、已触发次数]

请基于上面信息判断：
1. 当前情况是否需要采取行动？
2. 如果需要，调用哪个工具，参数是什么？
3. 如果不需要，输出一段 ≤ 50 字的判断理由。

注意事项：
- [明确告诉它什么时候不要触发，避免误报]
- [明确告诉它边界条件，如"1小时内不重复触发"]
- [明确告诉它优先级]
```

---

## 模板 3 · 错误处理标准化

> 场景：给已有代码统一加健壮的错误处理，不手写框架。

```
给 [目录/文件] 下所有外部调用（HTTP / RPC / LLM）加上统一的错误处理，要求：

1. 所有外部调用包 try/catch：失败自动重试 1 次（间隔 1 秒 + 随机抖动），
   仍失败就跳过本轮、打一条带上下文的 warn 日志，绝不让进程崩。

2. 有速率限制的 API（如 [API名称]）：轮询间隔不低于 60 秒；
   收到 HTTP 429 时本轮直接跳过，并把下一次请求推迟 5 分钟。

3. 任何 catch 都不允许吞掉错误不打日志。

4. 主循环最外层再包一层 try/catch，保证单轮失败不影响下一轮。

5. 不引入新依赖，日志用 console 即可，格式：[module] message

改完告诉我怎么人为制造一次失败来验证"打了日志 + 跳过 + 没崩"。
```

---

## 模板 4 · Dashboard 视觉升级

> 场景：把已跑通的 Dashboard 从"能用"升级到"作品集级"。

```
@[dashboard文件路径] 当前 Dashboard 跑通了，但视觉还可以更"作品集级"。
按下面 5 条规则升级：

1. 配色压缩到 1 主色（[如 indigo-500]）+ 1 强调色（仅用在告警状态）+ [如 slate-950] 背景。
   去掉所有其他色块。

2. 间距统一：所有 Card 用 p-6、grid gap 用 gap-4、容器 mx-auto max-w-7xl。

3. 字号统一 4 级：text-2xl (页面标题) / text-lg (卡片标题) / text-sm (正文) / text-xs (标签)。
   任何不在这 4 级的字号删掉。

4. 关键数字用 text-3xl font-bold，下面跟一行 text-sm text-slate-400 的标签。

5. 状态用颜色 + 图标（全部用 lucide-react，不要 emoji）：
   - [状态A] → [颜色] + [图标名]
   - [状态B] → [颜色] + [图标名]

每改一处用代码注释标 "[v2]"，方便对比。
```

---

## 模板 5 · 静态 Landing Page

> 场景：项目 URL 第一眼看到的页面，目标是 10 秒内让评委 / 用户懂这个产品。

```
@[landing文件路径] 我要把这个文件做成项目的 Landing 说明页。
目标：评委打开链接 10 秒内能懂项目是什么、给谁用、为什么选它。

结构从上到下：

1. Hero 区：
   - 项目名（大字）+ Logo（没有就用首字母色块占位）
   - Tagline：[你的 tagline，5-10 字]
   - One-liner：[你的一句话定位]
   - 主按钮 "[行动号召]" 链接到 [目标路径]
   - 次按钮 "GitHub" 链接到 [仓库地址]

2. How it works：3 步，每步一句话 + 一个 lucide-react 图标
   - 步骤1：[一句话，说"发现/输入"]
   - 步骤2：[一句话，说"处理/决策"]
   - 步骤3：[一句话，说"输出/结果"]

3. 差异化 callout 区：[你的核心差异点，1-2 句话]

4. 技术标签行：小 badge 列出 [技术栈清单]

设计要求：
- dark mode，主色 [如 indigo-500]，背景 [如 slate-950]
- 字号 ≤ 4 级（同模板4）
- 居中 max-w-5xl mx-auto，留白充足
- 整页是静态 Server Component，不 fetch 任何数据
```

---

## 模板 6 · 叙事资产重构

> 场景：黑客松 / 路演 / Demo Day 前需要一套打动评委的项目叙事。

```
你的角色：资深产品经理 + 黑客松评委（10+ 场经验）

任务：给我的项目重构 4 个核心叙事资产：
1. 项目名（≤ 20 字符）
2. Tagline（5-10 字）
3. One-liner（≤ 30 英文词 / 60 中文字）
4. Pitch 开头 60 秒口播脚本

我的项目情况：
- 核心闭环（3-5 句）：[说清楚：谁用 / 做了什么 / 结果是什么]
- 核心技术：[列出]
- 最关键的差异点：[如"先签字后结算" / "零配置自动发现"]
- 目标受众：[越具体越好，如"赛中交易员"]

给我 3 个候选，角度分别是：
- 候选 A：痛点优先（先讲用户有多痛）
- 候选 B：角色优先（把产品人格化成一个角色）
- 候选 C：类比优先（用大众熟悉的事物类比）

约束：
- Pitch 60s 前 10 秒只讲用户场景，不出现任何技术词
- 全部中英双语输出

最后评估：哪个最有"评委一秒 get 到"的力量，为什么。
```

---

## 模板 7 · README 评审 + 重写

> 场景：提交前用"苛刻评委视角"审查 README，再生成评委级版本。

```
你的角色：苛刻黑客松评委 + Grant Council 成员

输入：@README.md

按下面 4 个维度审查，每条给出：体感分（1-5）/ 扣分理由 / 1 句话改进建议：

1. 打开 GitHub，前 30 秒，能不能确定要不要继续看？
2. 快速扫 How it Works，能不能 1 分钟内理解架构？
3. Why Us + Roadmap，够不够说服 Grant Council 这个团队能 follow through？
4. Sponsor 评委视角：有没有"为什么我家技术栈是最优解"这一段？

审查完，输出修改后的完整 README（8 模块结构）：
1. One-liner + Quick Links（Live Demo / Contract / 运行状态）
2. 问题与解法（用户驱动叙事，≤ 6 行）
3. Demo（截图 + Live URL + 链上地址）
4. How it Works（Mermaid flowchart 架构图 + sequenceDiagram 时序图）
5. Tech Stack（表格含 Why 列：为什么选这个，不选替代品）
6. Why Now + Why Us（含团队可信度信号）
7. Roadmap（Done / Next 4w / 3-6m，含成功指标）
8. Links + License

约束：每段 ≤ 6 行；技术细节链接到 docs/ 子文档；
合约地址用 [0xabcd...1234] 格式 + Etherscan 链接。
```

---

## 模板 8 · Demo 视频脚本

> 场景：黑客松要求提交 3-5 分钟 Demo 视频，需要完整分镜脚本。

```
你的角色：擅长 Web3 hackathon Demo 视频导演

输入：
- 项目 README：@README.md
- Pitch 60s：[贴上你的口播脚本]
- 5 个可验证的证据点：
  1. [触发条件，如"价格异动检测"]
  2. [AI 决策可视化，如"终端日志显示 LLM 推理"]
  3. [链上 TX，如"Etherscan PredictionMade 事件"]
  4. [通知，如"Telegram 推送"]
  5. [结果面板，如"Dashboard 胜率更新"]

输出：3 分钟 Demo 视频完整脚本，4 段结构：
- 0:00-0:30 Hook
- 0:30-2:00 Demo（按上面 5 个证据点依次展示）
- 2:00-2:30 技术深度（架构图 + 关键代码各 15 秒）
- 2:30-3:00 结尾收场

每段必须写明：[Screen] 屏幕上显示什么 / [Voice] 说什么 / [Action] 具体操作

约束：
- 0-30s 不出现任何技术词
- AI 决策那段必须有"AI 在思考"的视觉化（如终端日志逐行出现）
- 链上 TX 必须出现在 1:10-1:30
- 总时长 2:55-3:00，不超过 3 分钟
```

---

## 速查：什么情况用哪个模板

| 阶段 | 场景 | 用哪个 |
|---|---|---|
| 启动 | 从零搭项目 | 模板 1 |
| 开发 | 加 AI 决策逻辑 | 模板 2 |
| 开发 | 加错误处理 | 模板 3 |
| 开发 | 前端视觉升级 | 模板 4 |
| 开发 | 做落地页 | 模板 5 |
| 提交前 | 打磨项目叙事 | 模板 6 |
| 提交前 | 写/改 README | 模板 7 |
| 提交前 | 录 Demo 视频 | 模板 8 |

---

> 最高频用到的两个：
> - **模板 2**（决策函数）— 有了它，任何数据 + 任何动作都能接 LLM 决策
> - **模板 6**（叙事资产）— 有了它，任何 idea 都能在 30 分钟内有一套能讲的 pitch
